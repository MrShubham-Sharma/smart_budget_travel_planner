# Python cache
__pycache__/
*.pyc
*.pyo

# Local database (never commit)
users.db
instance/

# ML model grids (auto-generated on startup via train_models.py, but now committed since they are small RF models)
# models/

# Environment variables (never commit secrets)
.env

# OS junk
.DS_Store
Thumbs.db
