# Python cache
__pycache__/
*.pyc
*.pyo

# Local database (never commit — Railway uses PostgreSQL)
users.db
instance/

# Large ML grid files (auto-generated at build time by train_models.py)
models/budget_grid.json
models/eta_grid.json

# Keep the trained RF model (budget_rf.pkl) committed — it's used at runtime

# Environment variables (never commit secrets)
.env

# OS junk
.DS_Store
Thumbs.db
